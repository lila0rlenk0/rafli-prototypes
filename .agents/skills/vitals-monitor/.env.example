# Sentry project identifiers (non-secret)
# Auth handled by `sentry` CLI session -- run `sentry auth login` to authenticate
SENTRY_ORG=""
SENTRY_PROJECT=""

# Vercel project auto-detected from .vercel/project.json
# Auth handled by `vercel` CLI session -- run `vercel login` to authenticate
